---
 - name: Jinja templating
   hosts: all
   vars_files:
     - var.yml

   tasks: 
     - name: copy the template
       template:
         src: template.j2
         dest: i_am_god/template
           # validate: [cmnd_to_validate]
           # backup: yes
