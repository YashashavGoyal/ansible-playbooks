[defaults]
inventory=./inventory.yml
remote_user=admin

# vault_password_file = key

ask_pass=no
host_key_checking=no


[privilege_escalation]
become=true
become_user=root
become_method=sudo
become_ask_pass=no
