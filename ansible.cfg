[defaults]
remote_user = admin
inventory = ./inventory.yml

roles_path = roles/path
collections_path = ./collection/path
vault_password_file = file.txt

ask_pass = no
host_key_checking = no

[privilege_escalation]
become = true
become_user = root
become_mthod = sudo
become_ask_pass = no
